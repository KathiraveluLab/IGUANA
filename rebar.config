{erl_opts, [debug_info]}.

%% src/ is the primary dir (required for iguana.app.src discovery).
%% src/erlang/ holds all Erlang module source files.
{extra_src_dirs, ["src/erlang"]}.

{deps, [
    %% ErlPort: enables the Erlang Master Controller (iguana_hf_controller) to
    %% spawn and communicate bidirectionally with the Python GPU worker process.
    {erlport, "0.10.1"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]},
        {extra_src_dirs, ["src/erlang"]},
        {deps, [{erlport, "0.10.1"}]}
    ]}
]}.

{eunit_opts, [verbose]}.
{eunit_tests, [iguana_entropy_guard_test]}.
